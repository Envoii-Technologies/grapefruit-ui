import { Canvas, Meta, Story, ArgsTable } from '@storybook/addon-docs'
import { FormNotification } from './FormNotification';

export const Template = (args) => (
	// <div style={{ width: "70vw", display : 'flex'}}>
		<FormNotification { ...args } />
	// </div>
);

<Meta title='Components/FormNotification' component={ FormNotification } />

# FormNotification

...

## Examples

### Default

...

<Canvas>
	<Story
		name="Default"
		args={{
			
		}}
	>
		{ Template.bind({}) }
	</Story>
</Canvas>

### Notification Message

...

<Canvas>
	<Story
		name="message"
		args={{
			message: "This is a custom message!"
		}}
	>
		{ Template.bind({}) }
	</Story>
</Canvas>

### Notification Type

...

<Canvas>
	<Story
		name="type success"
		args={{
			type: "success",
			message: "This is a success message!"
		}}
	>
		{ Template.bind({}) }
	</Story>
	<Story
		name="type warning"
		args={{
			type: "warning",
			message: "This is a warning message!"
		}}
	>
		{ Template.bind({}) }
	</Story>
	<Story
		name="type error"
		args={{
			type: "error",
			message: "This is an error message!"
		}}
	>
		{ Template.bind({}) }
	</Story>

</Canvas>

## API

<ArgsTable of={ FormNotification } />
