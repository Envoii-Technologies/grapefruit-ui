import { Canvas, Meta, Story, ArgsTable } from '@storybook/addon-docs'
import { PopOver } from './PopOver';
import { Button } from './../Button';

export const Template = (args) => (
	<PopOver { ...args }><Button label="Click Me!"/></PopOver>
);

<Meta title='Components/PopOver' component={ PopOver } />

# PopOver

...

<Story
	name="Example"
	args={{
		text: "This is a PopOver"
	}}
>
	{ Template.bind({}) }
</Story>

<br/>

## Examples

### Default

...

<Canvas>
	<Story
		name="Default"
		args={{
			
		}}
	>
		{ Template.bind({}) }
	</Story>
</Canvas>

### Position

...

<Canvas>
	<Story
		name="Position Top"
		args={{
			position: "to"
		}}
	>
		{ Template.bind({}) }
	</Story>
	<Story
		name="Position Right"
		args={{
			position: "right"
		}}
	>
		{ Template.bind({}) }
	</Story>
	<Story
		name="Position Bottom"
		args={{
			position: "bottom"
		}}
	>
		{ Template.bind({}) }
	</Story>
	<Story
		name="Position Left"
		args={{
			position: "left"
		}}
	>
		{ Template.bind({}) }
	</Story>
</Canvas>

### Options

...

<Canvas>
	<Story
		name="Custom Options"
		args={{
			position: "bottom",
			options: [
        {
            label: "Edit",
            type: "secondary",
            action: () => console.log("test")
        },
        {
            label: "Delete",
            type: "error",
            action: () => console.log("test2")
        }
    ]
		}}
	>
		{ Template.bind({}) }
	</Story>
</Canvas>

## API

<ArgsTable of={ PopOver } />
