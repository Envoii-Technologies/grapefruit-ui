import { Canvas, Meta, Story, ArgsTable } from '@storybook/addon-docs'
import { PageMenu } from './PageMenu';

export const ExampleTemplate = (args) => (
	<div style={{ height: 300, display: "flex" }}>
		<PageMenu { ...args } />
	</div>
);

export const Template = (args) => (
	<div style={{ height: 300 }}>
		<PageMenu { ...args } />
	</div>
);

<Meta title='Components/PageMenu' component={ PageMenu } />

# PageMenu

...

<Story
	name="Example"
	args={{
		
	}}
>
	{ ExampleTemplate.bind({}) }
</Story>

## Examples

### Default

...

<Canvas>
	<Story
		name="Default"
		args={{
			
		}}
	>
		{ Template.bind({}) }
	</Story>
</Canvas>

### isMenuExpanded

<Canvas>
	<Story
		name="Expanded Menu"
		args={{
			
		}}
	>
		{ Template.bind({}) }
	</Story>
</Canvas>

<Canvas>
	<Story
		name="Compressed Menu"
		args={{
			isMenuExpanded: false
		}}
	>
		{ Template.bind({}) }
	</Story>
</Canvas>

### isUserMenuExpanded

<Canvas>
	<Story
		name="Expanded User Menu"
		args={{
			isUserMenuExpanded: false
		}}
	>
		{ Template.bind({}) }
	</Story>
</Canvas>

<Canvas>
	<Story
		name="Compressed User Menu"
		args={{
			isUserMenuExpanded: true
		}}
	>
		{ Template.bind({}) }
	</Story>
</Canvas>

## API

<ArgsTable of={ PageMenu } />
