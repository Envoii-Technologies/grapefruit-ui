import { Canvas, Meta, Story, ArgsTable } from '@storybook/addon-docs'

import mockUser from './../../data/mockUser';
import mockMenu from './../../data/mockMenuItems';

import { PageMenu } from './PageMenu';

export const ExampleTemplate = (args) => (
	<div style={{ height: 350, width: 500, display: "flex" }}>
		<PageMenu { ...args } userData={mockUser.userData} menuData={mockMenu.admin} isMenuExpanded={true}/>
	</div>
);

export const Template = (args) => (
	<div style={{ height: 350 , width: 500 }}>
		<PageMenu { ...args } userData={mockUser.userData} menuData={mockMenu.admin} isMenuExpanded={true}/>
	</div>
);

<Meta title='Components/PageMenu' component={ PageMenu } />

# PageMenu

...

<Story
	name="Example"
	args={{
		
	}}
>
	{ ExampleTemplate.bind({}) }
</Story>

## Examples

### Default

...

<Canvas>
	<Story
		name="Default"
		args={{
			
		}}
	>
		{ Template.bind({}) }
	</Story>
</Canvas>

### isMenuExpanded

<Canvas>
	<Story
		name="Expanded Menu"
		args={{
			
		}}
	>
		{ Template.bind({}) }
	</Story>
</Canvas>

<Canvas>
	<Story
		name="Compressed Menu"
		args={{
			isMenuExpanded: false
		}}
	>
		{ Template.bind({}) }
	</Story>
</Canvas>

### isUserMenuExpanded

<Canvas>
	<Story
		name="Expanded User Menu"
		args={{
			isUserMenuExpanded: false
		}}
	>
		{ Template.bind({}) }
	</Story>
</Canvas>

<Canvas>
	<Story
		name="Compressed User Menu"
		args={{
			isUserMenuExpanded: true
		}}
	>
		{ Template.bind({}) }
	</Story>
</Canvas>

## API

<ArgsTable of={ PageMenu } />
