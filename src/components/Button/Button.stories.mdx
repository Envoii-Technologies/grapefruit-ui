import { Canvas, Meta, Story, ArgsTable } from '@storybook/addon-docs'
import { Button } from './Button';

export const Template = (args) => (
	<Button { ...args } />
);

<Meta title='Components/Button' component={ Button } />

# Button

A Component for user interaction on click.

<Story
	name="Example"
	args={{
		
	}}
>
	{ Template.bind({}) }
</Story>

<br />

## Examples

### Default

By default, the Button component is set to Primary, displays the text "Default" and is of Medium size.

<Canvas>
	<Story
		name="Default"
		args={{
			
		}}
	>
		{ Template.bind({}) }
	</Story>
</Canvas>

### Label

By passing a `string` to the `label` property, the text displayed on the Button component can be changed.

<Canvas>
	<Story
		name="Custom Label"
		args={{
			label: 'Button!'
		}}
	>
		{ Template.bind({}) }
	</Story>
</Canvas>

### Size

The size of the Component can be changed by passing either `small`, `medium` or `large` as `string` to the `size` property.

<Canvas>
	<Story
		name="Small Size"
		args={{
			size: "small"
		}}
	>
		{ Template.bind({}) }
	</Story>
	<Story
		name="Medium Size"
		args={{
			size: "medium"
		}}
	>
		{ Template.bind({}) }
	</Story>
	<Story
		name="Large Size"
		args={{
			size: "large"
		}}
	>
		{ Template.bind({}) }
	</Story>
</Canvas>

## API

<ArgsTable of={ Button } />
