import { Canvas, Meta, Story, ArgsTable } from '@storybook/addon-docs'
import { LoadingSpinner } from './LoadingSpinner';

export const Template = (args) => (
	<LoadingSpinner { ...args } />
);

<Meta title='Components/LoadingSpinner' component={ LoadingSpinner } />

# LoadingSpinner

...

## Examples

### Default

...

<Canvas>
	<Story
		name="Default"
		args={{
			
		}}
	>
		{ Template.bind({}) }
	</Story>
</Canvas>

## API

<ArgsTable of={ LoadingSpinner } />
